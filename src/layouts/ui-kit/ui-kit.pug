include /common/logo/logo

block variables

doctype html
html(lang="en")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title= title
  body(class=`${pageClass} ${modifierClass}`)
    header.page-header
      .page-header__logo
        +logo-mini
    .page-wrapper_ui-kit
      main(class=`${pageClass}__content`)
        block content