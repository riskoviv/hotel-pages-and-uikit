mixin rateButton(options)
  .rate-button(id=options.id)
    - var n = 0;
    while n < 5
      span.material-icons.rate-button__star(
        id=options.id + '_' + ++n
      )= (n <= options.rating) ? 'star' : 'star_border'