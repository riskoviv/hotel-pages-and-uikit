mixin likeButton(id, count, isClicked)
  .like-button(class=isClicked ? 'like-button_clicked' : '')
      .like-button__body
        span.like-button__heart.material-icons favorite_border
        span.like-button__count= count
