mixin textField(options)
  .control-container.text-field(class=options.class ? 'text-field_' + options.class : '')
    .control-container__caption
      if options.title
        h3.control-container__title.text-field__title= options.title
      if options.description
        span.control-container__description= options.description
    input.text-field__input(
      type=options.type || 'text'
      placeholder=options.placeholder
      value=options.value
      class=options.class ? 'text-field_' + options.class + '__input' : ''
      class=options.forceHoverStyle ? 'force-focus-hover' : ''
      maxlength=options.class=='masked' ? '10' : ''
    )
    if options.class == 'subscription'
      button.text-field_subscription__button.material-icons(type='button') arrow_forward