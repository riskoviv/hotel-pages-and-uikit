mixin checkbox(id, label, isChecked)
  .input-button.checkbox
    if isChecked
      input(id=id, type='checkbox', checked).switch.checkbox__switch
    else
      input(id=id, type='checkbox').switch.checkbox__switch
    label(for=id).input-button__label.checkbox__label= label